import devConfig from './environments/config.dev';
import prodConfig from './environments/config.prod';
import stagingConfig from './environments/config.staging';

let config = '';
config = prodConfig;
export default config;
